# Runtime dependencies. Mirrors [project.dependencies] in pyproject.toml.
# Development tooling lives in the `dev` extra: `uv run --extra dev ...`.
PyYAML>=6.0
Pillow>=9.0.0
boto3>=1.26.0
rich>=13.0.0
